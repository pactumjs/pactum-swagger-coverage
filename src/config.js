const config = {
  name: 'SwaggerCovReporter',
  reportPath: './reports',
  reportFile: 'swagger-cov-report.json',
  swaggerJsonUrl: "",
  swaggerYamlPath: "",
  basePath: "",
  oasTag: "swagger"
}

module.exports = config;
